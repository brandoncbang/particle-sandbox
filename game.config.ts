import { GameConfig } from "./src/game/config";

export const config: GameConfig = {
  rendering: {
    scale: 2,
    backgroundColor: "cornflowerblue",
  },
  world: {
    width: 300,
    height: 200,
    boundary: "solid",
  },
};
